# Stage 4.5 — Concatenate Sections

**Entry condition:** `current_stage: 4.5` (auto-advancing from stage 4).
**Purpose:** Assemble the 6 section files into the final `docs/guides/<category>.md`.

## 1. Mark in-progress

Update state:
```yaml
current_stage: 4.5
stage_status: in_progress
last_run_at: <now>
```
Append to run log: `- <datetime> — Stage 4.5 started: assembling sections`

## 2. Verify all section files

Read each file listed in `state.section_files`. Check:
- File exists on disk
- File is > 1000 bytes
- File starts with `## ` (an H2 heading)

If any section file fails this check: stop with:
```
✗ Stage 4.5 failed: section file <path> is missing or malformed.
Re-run stage 4 first: set current_stage to 4 and stage_status to failed in the state file,
then run /build-pathway <category>.
```

## 3. Assemble the guide

Build the final guide by concatenating:

1. **Header block** (generate fresh — do not copy from a template):
   ```markdown
   <!-- Generated by build-pathway skill. Category: <category>. Assembled: <datetime>. -->
   ```

2. **Section 1 content** — full text of `docs/guides/<category>.section-01-destinations.md`
3. Blank line separator
4. **Section 2 content** — full text of `docs/guides/<category>.section-02-documents.md`
5. Blank line separator
6. **Section 3 content** — full text of `docs/guides/<category>.section-03-costs.md`
7. Blank line separator
8. **Section 4 content** — full text of `docs/guides/<category>.section-04-visa-routes.md`
9. Blank line separator
10. **Section 5 content** — full text of `docs/guides/<category>.section-05-scams.md`
11. Blank line separator
12. **Section 6 content** — full text of `docs/guides/<category>.section-06-contacts.md`

Write the assembled result to: `docs/guides/<category>.md`

## 4. Verify the final guide

Read `docs/guides/<category>.md` back and confirm:
- File size > 8000 bytes
- Contains exactly 6 H2 headings (`## `)
- All 6 H2 headings match the guide-template.md headings:
  - `## 1. Destination Options` (or equivalent — check guide-template.md for exact wording)
  - `## 2. Step-by-Step Document Checklist`
  - `## 3. Realistic Costs`
  - `## 4. Visa Route Overview`
  - `## 5. Scam Red Flags`
  - `## 6. Legitimate Contacts & Official Links`
- No `{{PLACEHOLDER}}` text remains
- No section is empty (each H2 is followed by at least 200 characters of content before the next H2)

If verification fails: stop with a specific error describing which check failed.

## 5. Update state and auto-advance

Update state:
```yaml
current_stage: 4.5
stage_status: completed
synthesis_doc: "docs/guides/<category>.md"
```
Append to run log: `- <datetime> — Stage 4.5 complete: guide assembled at docs/guides/<category>.md`

**Auto-advance:** Do NOT hard-exit. Immediately proceed to Stage 5 handler (`stage-5-review.md`).
