---
plan: r495-paid-tier
status: planned
started: 2026-05-16
model-execute: sonnet
git-hash-at-plan: aed5ca02492e38f0968c5c46e9c2f2c4ae810d2b
---

# Progress: R495 Paid Tier

## Status

| #  | Step                                                                         | Status   | Depends on   | Section file |
|----|------------------------------------------------------------------------------|----------|--------------|--------------|
| 01 | DB migration: profiles.tier + paid_email_credits, bookings + paid_reports    | done     | —            | [step-01-db-migrations.md](sections/step-01-db-migrations.md) |
| 02 | Personal-context section in teaching (2 new Qs; 3 already existed)            | done     | —            | [step-02-personal-context-section.md](sections/step-02-personal-context-section.md) |
| 03 | Scoring engine + **teaching.json** rubric (healthcare/farming deferred)       | done     | —            | [step-03-scoring-engine.md](sections/step-03-scoring-engine.md) |
| 04 | /members/[category]/score page                                                | done     | 02, 03       | [step-04-score-page.md](sections/step-04-score-page.md) |
| 05 | AssessmentWizard: redirect to /score on submit + re-entry                     | done     | 04           | [step-05-wizard-redirect.md](sections/step-05-wizard-redirect.md) |
| 06 | Payment provider abstraction (Paystack chosen)                                | done     | 01           | [step-06-payment-provider.md](sections/step-06-payment-provider.md) |
| 07 | Checkout API + webhook → flip tier                                            | done     | 06           | [step-07-checkout-api.md](sections/step-07-checkout-api.md) |
| 08 | PDF report generator (@react-pdf/renderer + Storage cache)                    | done     | —            | [step-08-pdf-report.md](sections/step-08-pdf-report.md) |
| 09 | Paid dashboard surfaces: report link, book, follow-up form                    | done     | 01, 08       | [step-09-paid-dashboard.md](sections/step-09-paid-dashboard.md) |
| 10 | /members/[category]/book — Cal.com + POPIA consent                            | done     | 01, 07       | [step-10-booking-consent.md](sections/step-10-booking-consent.md) |
| 11 | Follow-up send + atomic credit decrement                                      | done     | 01, 07, 09   | [step-11-follow-up-email.md](sections/step-11-follow-up-email.md) |
| 12 | Eager report gen on webhook + email PDF + WhatsApp notification                | done     | 07, 08       | [step-12-email-report.md](sections/step-12-email-report.md) |
| 13 | Playwright regression                                                         | done     | 04, 09, 11   | [step-13-playwright-tests.md](sections/step-13-playwright-tests.md) |

## Legend

- ⏳ `blocked` — waiting on dependencies
- 🟢 `ready` — all dependencies met, can start
- 🔄 `in-progress` — currently being implemented
- ✅ `done` — implemented and verified
- ❌ `failed` — step failed during execution
- ⏭️ `skipped` — intentionally not done

## Completion Log

| # | Completed | Summary |
|---|-----------|---------|
| 01 | 2026-05-17 | Wrote `supabase/migrations/20260516_paid_tier_tables.sql` and applied via Supabase SQL editor. Profiles gained tier/paid_email_credits/last_payment_ref; bookings + paid_reports tables created with RLS; private `paid-reports` storage bucket + defense-in-depth RLS on storage.objects; decrement/increment_email_credit fns granted to service_role only. Verify block returned all expected rows. |
| 02 | 2026-05-17 | Added `situation` step to `lib/assessments/steps/teaching.ts` with 2 new fields (`situation.family_status`, `situation.current_employment`). Skipped 3 of plan's 5 Qs as duplicates of existing teaching fields (`documents.dependants_count`, `documents.available_capital`, `readiness.target_timeline`) — scoring engine will reference those existing IDs directly. No shared `situation.ts` module created (YAGNI — teaching-only pilot). Schema is permissive, no whitelist update needed. Build passes. |
| 03 | 2026-05-17 | Created `lib/scoring/types.ts` (Rubric, Rule variants, ScoreResult), `lib/scoring/index.ts` (`loadRubric` switch-based, `calculateScore` pure fn supporting match/range/present/count rule types with proper clamping), and `lib/scoring/rubrics/teaching.json` (5 dimensions: credentials 0.30, experience 0.20, language 0.15, documents 0.20, readiness 0.15; weights sum to 1.0; references real teaching field IDs). Smoke-tested: strong applicant → 99 `strong_potential`, weak → 28 `high_blockers`, deterministic across runs, `loadRubric('healthcare')` returns null. Build passes. |
| 04 | 2026-05-17 | Created `lib/scoring/teasers.ts` (gpt-4o-mini, JSON-mode, system prompt forces dimension naming, graceful fallback to raw rubric reason if API fails / key missing / JSON parse fails), `components/ScoreResult.tsx` (banded card with accent colour per band: red/gold/green; paywall block for free tier with R495 CTA → `/checkout`, dashboard CTA for paid tier), and `app/members/[category]/score/page.tsx` (server page, gated by requireProfile + category match, redirects rubricless categories to `/dashboard`, redirects users without submitted assessment to `/assessment`, parses assessment.data through zod schema for type narrowing, reads `profiles.tier` for paid/free branch). **End-to-end verified in browser via Playwright** — seeded strong-applicant teaching user, navigated to `/members/teaching/score`, page rendered with band="Strong potential", strength="Your strongest factor is **credentials** — your B.Ed Honours is well-regarded internationally.", blocker="Your biggest blocker is **language** — you have no English test on record, which UK and Middle East roles will require.", paywall CTA visible. Seed script at `scripts/seed-score-test-user.ts`. |
| 06 | 2026-05-17 | **Paystack chosen** (sandbox sk_test_ keys, no merchant approval needed). Created `lib/payments/types.ts` (PaymentProvider interface — initiateCheckout + verifyWebhook returning normalised `{externalRef, status, amountCents, userId, metadata}`), `lib/payments/paystack.ts` (PaystackProvider: POST /transaction/initialize with metadata.userId; verifyWebhook does HMAC-SHA512 of raw body with secret, timingSafeEqual, throws on missing/tampered sig or missing user), and `lib/payments/provider.ts` (`getPaymentProvider()` single-line swap point, server-only guard on the boundary). Updated `.env.example` to document PAYSTACK_SECRET_KEY + NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY + OPENAI_API_KEY. Smoke-tested signature verification — valid sig parses; tampered sig, missing sig, and tampered body all correctly throw. Build passes. |
| 13 | 2026-05-17 | Added `tests/helpers.ts` helpers `findUserIdByEmail`, `makePaid(userId, credits)`, `insertSubmittedTeachingAssessment(userId)` (seeds a "Strong potential" profile via service-role, skips the wizard form-fill entirely). Created `tests/paid-tier.spec.ts` with 11 tests across 4 describe blocks: score page (completed assessment → band + paywall; non-rubric category redirects to /dashboard; no submitted assessment redirects to /assessment); paid dashboard surfaces (report + book links + credits counter; free user sees nothing; follow-up decrement with Brevo mocked via `page.route('https://api.brevo.com/**')`; out-of-credits state at 0); booking page (free user redirects, paid sees consent + locked overlay); WhatsApp visibility (free sees pill, paid has zero wa.me links). Adapted plan's healthcare-based fixture to teaching (only rubric we ship per pilot scope). Full suite green: **42/42 passed** (31 existing auth-flow + member-page + 11 new paid-tier), 0 stale playwright+ users after run (cleanup via `deleteUser` in finally blocks). |
| 12 | 2026-05-17 | Installed `@vercel/functions` for `waitUntil`. Extended `lib/reports/generator.ts` to also return `{pdfBuffer, userEmail, userName, categoryLabel}` (auth email looked up via service-role `admin.getUserById`). Created `lib/notifications/report-ready.ts` (`generateAndNotify(userId)`): runs the generator → sends Brevo email to the buyer with PDF base64-attached, HTML body with personalised greeting + dashboard link + WhatsApp deep-link + follow-ups note; swallows errors so the dashboard's on-demand download remains the safety net. Modified `app/api/payments/webhook/route.ts` to call `waitUntil(generateAndNotify(evt.userId))` immediately after `applySuccessfulPayment` succeeds AND `result.flipped` is true (so duplicates don't double-generate); added `maxDuration=300` to honour Vercel Fluid Compute headroom. Also wired it into the paid-page fallback (`app/members/[category]/paid/page.tsx`) so a user landing before the webhook still triggers the email. Verified end-to-end: switched test user email to laudes.michael+jobabroad-test@gmail.com → reset tier=free + cleared paid_reports → curl-fired signed webhook → webhook responded 200 in 549ms (not blocked) → 6s later paid_reports row appeared with fresh timestamp → email arrived in Gmail at +6s with subject "Your full Teaching assessment report", `jobabroad-teaching-report.pdf` (17.5 KB) attached, and the personalised body with dashboard + WhatsApp links. **Known cosmetic limitation:** Brevo wraps the wa.me link in click-tracking proxy URLs (sendibt2.com) — still works via redirect, no functional break. |
| 11 | 2026-05-17 | Created `lib/email/brevo.ts` (REST API wrapper with `sendEmail({from,to,replyTo,subject,htmlContent})` — maps friendly `from` to Brevo's required `sender` field internally; HTML/header sanitisers exported) and `app/api/follow-up/send/route.ts` (POST, paid-only, BotID-gated, input validation + size caps, atomic decrement via service-role RPC `decrement_email_credit`, Brevo send with category-prefixed subject + reply-to=buyer, best-effort credit refund on Brevo failure). Env vars added: `BREVO_API_KEY`, `JOHN_INBOX_EMAIL`, plus optional `BREVO_FROM_EMAIL`/`BREVO_FROM_NAME` for swapping verified senders without code changes. Hit one bug live: Brevo API uses `sender` not `from` (their error message "sender is missing" was literal) — fixed in the wrapper. Verified end-to-end: spent 5 credits via UI form + direct fetch, all 5 emails landed in laudes.michael@gmail.com with correct from/subject/body (verified via gog gmail search), 6th attempt returned 403 `no_credits_remaining`, dashboard form swapped to "You've used all 5 follow-ups — Book a fresh call to chat again." Atomic decrement holds under back-to-back requests. |
| 10 | 2026-05-17 | Created `app/api/booking/consent/route.ts` (POST, paid-only, inserts `bookings` row with consented_at = now via service-role client; `consented_at NOT NULL` enforced at DB level from step 01). Created `app/members/[category]/book/page.tsx` (server page, paid+category-match gates with redirects to `/dashboard`) and `app/members/[category]/book/BookingClient.tsx` (consent checkbox using the exact POPIA one-liner; on tick POSTs `/api/booking/consent`; on success renders Cal.com inline embed via `@calcom/embed-react` pointing at `jobabroad/15min`; user's session email pre-fills the embed). Initial attempt to hand-roll the Cal.com init failed with `TypeError: Cannot convert undefined or null to object` — swapped to the official `@calcom/embed-react` package which handles the IIFE pattern correctly. Registered `/api/booking/consent` in instrumentation-client.ts BotID protect list. Verified in Playwright: paid user lands on `/members/teaching/book`, sees locked "Tick the consent checkbox" overlay, ticks consent → POST 200 → Cal.com calendar renders inline with real slots → `bookings` row inserted with `consented_at` timestamp populated. |
| 07.1 | 2026-05-17 | **Hardening: webhook fallback via provider verify.** Real Paystack test payment exposed a gap — if the dashboard Webhook URL is empty (or the webhook fails for any reason in production), users who paid get stuck on "Confirming payment…" forever. Added `verifyTransaction(reference)` to PaymentProvider interface + implemented on PaystackProvider (`GET /transaction/verify/{ref}`); extracted DRY helper `lib/payments/apply.ts::applySuccessfulPayment` used by both webhook handler AND the paid landing page; paid page now reads `?reference=` (Paystack also sends `trxref=`), calls `verifyTransaction`, and flips tier itself IFF: status=success + metadata.userId matches the current session (never trust query-param user IDs). Webhook still primary; this is belt-and-braces. Verified end-to-end: reset tier=free, hit `/members/teaching/paid?reference=15yynnx6sy` (real Paystack ref from John's manual test payment), page rendered "Payment confirmed", DB shows `tier=paid, credits=5, last_payment_ref='15yynnx6sy'`. |
| 07 | 2026-05-17 | Created `app/api/payments/checkout/route.ts` (POST, auth-gated, blocks already-paid users, calls `getPaymentProvider().initiateCheckout` with R495=49500 cents, returns `{checkoutUrl}`), `app/api/payments/webhook/route.ts` (POST, no auth — server-to-server; verifies signature → 401 on invalid; sanity-checks amount; idempotent via `profiles.last_payment_ref` check; on success updates tier='paid' + paid_email_credits=5 + last_payment_ref via service-role client), and `app/members/[category]/paid/page.tsx` (post-payment thank-you with "Open dashboard" + "Download report" CTAs; if tier hasn't flipped yet, shows "Confirming payment…" with 3s meta-refresh poll). Wired ScoreResult's paywall button to POST `/api/payments/checkout` and `window.location.href = checkoutUrl` (added 'use client' directive, loading state, error display). Registered `/api/payments/checkout` in instrumentation-client.ts BotID protect list; webhook deliberately NOT protected (server-to-server). Build passes. **Awaiting user-supplied Paystack test keys for live e2e.** |
| 09 | 2026-05-17 | Created `components/FollowUpForm.tsx` (client; subject + textarea + send button; shows "N of 5 left"; on submit POSTs to `/api/follow-up/send` and decrements local `remaining`; renders "You've used all 5 follow-ups — Book a fresh call to chat again." when credits=0). Extended `app/dashboard/page.tsx` to select `tier, paid_email_credits` and render a "Paid tier — Your full report & call" section above the existing 4-card grid when `tier='paid'`: dark-green "Download report (PDF)" button → `/api/reports/download`, outlined "Book your 15-min call" link → `/members/[category]/book` (step 10, not built yet), and the FollowUpForm below. Verified in Playwright: paid user lands on dashboard, sees full paid section, clicked Download → redirected to fresh 5-min signed Supabase Storage URL serving the PDF. Free users untouched. Build passes. |
| 08 | 2026-05-17 | Installed `@react-pdf/renderer`. Downloaded DM Sans Regular + Bold TTFs from gstatic to `public/fonts/`. Built `lib/reports/types.ts` (ReportData), `lib/reports/pdf-template.tsx` (A4 template with band pill, score breakdown bars, paragraphs, numbered actions, contacts, fixed footer with page numbers, page-number fix via `render` prop), and `lib/reports/generator.ts` (orchestrates: load profile + submitted assessment + rubric → score → corpus search via search-pathway edge fn keyed on worst-dim reasons → 3 parallel LLM calls for whatsWorking/whatsBlocking/nextActions, each with anti-hallucination guards + graceful fallback to raw rubric reasons → render → upload to private `paid-reports/{userId}/report-{ts}.pdf` → upsert paid_reports row → 5-min signed URL). LLM prompts force dimension naming + verbatim rule quotes. Added `app/api/reports/generate/route.ts` (POST, paid-only, regenerates) and `app/api/reports/download/route.ts` (GET, paid-only, serves cached or generates if missing, redirects to signed URL); both with `runtime='nodejs'` + `maxDuration=60`. Smoke-tested both strong (75/100) and weak (high_blockers) profiles end-to-end — **both 2 pages**, ~7s generation, PDF rendered with Unicode (R, ✓, em-dash all visible). Build passes. |
| 05 | 2026-05-17 | Wired `components/AssessmentWizard.tsx` post-submit `router.push('/members/<category>/score')` (with proper error-path handling — stays on wizard if submit POST fails); deleted `components/AssessmentConfirmation.tsx`; client-side `useEffect` for `initialStatus==='submitted'` defense-in-depth redirect. Also added **server-side redirect** to `app/members/[category]/assessment/page.tsx` — if `getLatestAssessment` returns a submitted row for this category, redirect immediately to `/score`. This was outside step 05's literal scope but the original Done When ("re-opening after submission redirects to /score") was unreachable without it because the assessment page hardcodes `initialStatus={null}`. Verified in Playwright: visiting `/members/teaching/assessment` for a submitted user now lands on `/members/teaching/score` server-side. **Also fixed draft-resume bug** discovered during testing — assessment page now passes the latest draft's `data/completed_step_slugs/id/status` to the wizard instead of hardcoded empty props, so users who navigate away mid-assessment resume at the correct step with their answers intact (verified in Playwright: filled step 1 → navigated to dashboard → returned → resumed at step 2, step 1 data preserved when clicking Back). Cleaned up situation step's `supportHint` which was being rendered as an inappropriate WhatsApp prompt. Build passes. |

## Next ready

Steps 02 (teaching personal-context), 03 (scoring engine + teaching.json), 08 (PDF report) — all foundational and independent. Step 01 done.

## Scope pivot (2026-05-17)

**Teaching-only pilot.** Ship the full paid-tier flow end-to-end for the `teaching` category only. Site remains private. Friends-and-family testing on teaching validates the flow before any other category's rubric is built. Steps 02/03/04 narrowed accordingly; ScoringComingSoon component is NOT built in this iteration. Other 10 categories: separate plans, post-validation.

## Pre-flight checklist (user action needed)

Before step 06: confirm which payment provider (Paystack or PayFast) approved your merchant account.
Before step 10: create a Cal.com event "Jobabroad intro" (15 min) and note the embed slug.
Before step 11: generate a Brevo REST API key (different from the SMTP key) and set `BREVO_API_KEY` + `JOHN_INBOX_EMAIL` in `.env.local`.
